clc
close all
Fs = 250;
signal = readmatrix('XX02.csv');
myAnn = singleqrs(signal',Fs);

t = linspace(0,numel(signal)/Fs,numel(signal));

figure;
plot(t,signal);
xlabel('time (s)');
ylabel('A');
hold on;
plot(myAnn
